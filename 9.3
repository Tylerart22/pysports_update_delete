insert_query = ("INSERT INTO player (first_name, last_name, team_id) "
                "VALUES (%s, %s, %s)")

player_data = ("Gandalf", "The Grey", 1)
cursor.execute(insert_query, player_data)
db.commit()

select_query = ("SELECT player_id, first_name, last_name, team_name "
                "FROM player "
                "INNER JOIN team ON player.team_id = team.team_id")

cursor.execute(select_query)

players = cursor.fetchall()

for player in players:
  print(f"Player ID: {player[0]} - Name: {player[1]} {player[2]} - Team: {player[3]}")
  update_query = "UPDATE player SET first_name = %s WHERE first_name = 'johnson' AND last_name = 'johnson'"
new_name = ("j2",)
cursor.execute(update_query, new_name)
db.commit()
